{
  "_comment": "im-agentproc 质量门（flowcast 格式）。对齐 .github/workflows/publish.yml（cargo check --all-targets + cargo test --all-targets）。纯 Rust crate，无 desktop frontend。",
  "gates": {
    "fmt": {
      "cmd": "cargo fmt --all -- --check",
      "onFail": "autofix",
      "autofixCmd": "cargo fmt --all"
    },
    "clippy": {
      "_comment": "零 warning 容忍，对齐 ilink-hub 惯例。",
      "cmd": "cargo clippy --all-targets --all-features -- -D warnings",
      "onFail": "resume-fix",
      "timeout": 300000
    },
    "test": {
      "_comment": "对齐 publish.yml 的 cargo test --all-targets。",
      "cmd": "cargo test --all-targets --all-features",
      "onFail": "resume-fix",
      "timeout": 600000
    },
    "build": {
      "cmd": "cargo build --all-features",
      "onFail": "rollback",
      "timeout": 300000
    }
  }
}
